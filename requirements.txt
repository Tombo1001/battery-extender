# Direct dependencies — pinned for supply chain security.
# To update: pip install --upgrade textual psutil && pip freeze > requirements.txt
# Then audit: pip install pip-audit && pip-audit -r requirements.txt
textual==8.2.7
psutil==7.2.2

# Transitive dependencies (pinned to lock the full graph)
linkify-it-py==2.1.0
markdown-it-py==4.2.0
mdit-py-plugins==0.6.1
mdurl==0.1.2
platformdirs==4.9.6
pygments==2.20.0
rich==15.0.0
typing-extensions==4.15.0
uc-micro-py==2.0.0
